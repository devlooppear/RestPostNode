{
  "name": "restpostgresnode",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "vercel-build": "npx prisma generate && npx prisma migrate deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pg": "^8.11.1"
  },
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  }
}
